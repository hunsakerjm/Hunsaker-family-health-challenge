{
  "name": "hunsaker-family-backup",
  "private": true,
  "description": "Standalone, undeployed Worker for scheduled D1 -> R2 backups. Separate from the root Pages project's package.json — see README.md before deploying.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.125.0",
    "@cloudflare/workers-types": "^4.20260823.0"
  }
}
